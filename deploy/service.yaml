apiVersion: v1
kind: Service
metadata:
  name: parent-bot
  labels:
    app: backend
spec:
  ports:
    - port: 80
      protocol: TCP
  selector:
    app: backend

---
apiVersion: v1
kind: Service
metadata:
  name: child-bot
  labels:
    app: backend
spec:
  ports:
    - port: 81
      protocol: TCP
  selector:
    app: backend
